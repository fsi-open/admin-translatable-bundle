{% extends '@FSiAdmin/CRUD/create.html.twig' %}

{% block content %}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                <h3 id="page-header">{{ title|trans({}, 'FSiAdminBundle') }}</h3>
                {{ form_start(form, { action: path('fsi_admin_translatable_crud_create', { element: element.id, locale: element.locale }), 'attr': { class: 'form-horizontal' }|merge(form.vars.attr) }) }}
                {{ form_widget(form) }}
                <div class="form-group">
                    <div class="col-lg-offset-2 col-lg-10">
                        <button type="submit" class="btn btn-default">{{ 'crud.create.button.save'|trans({}, 'FSiAdminBundle')  }}</button>
                        <button type="reset" class="btn btn-primary">{{ 'crud.create.button.reset'|trans({}, 'FSiAdminBundle')  }}</button>
                        {% if app.request.query.has('redirect_uri') %}
                            <a class="btn btn-info pull-right" href="{{ app.request.query.get('redirect_uri') }}">{{ 'crud.create.button.back_to_list'|trans({}, 'FSiAdminBundle')  }}</a>
                        {% endif %}
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}